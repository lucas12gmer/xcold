<?php
    //MySQL settings
    $dbhost = "localhost";
    $dbuser = "root";
    $dbpassword = "";
    $dbname = "";
    $dbport = 3306;

    // Minecraft server settings for socket communication
    $host = 'localhost'; // Minecraft server host
    $port = 42070; // Port for ban/unban messages (can be configured in the settings.yml in bungeecord plugin)

    // Server mode (true for online mode, false for offline)
    $onlineMode = true; // Use UUIDs (true) or names (false)
?>